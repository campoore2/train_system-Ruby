<h1>Trains</h1>
<% if @train.any?() %>
  <ul>
    <% @trains.each() do |train| %>
      <li><a href="/rider_info<%= train.id() %>"><%= train.line %></a></li>
    <% end %>
  </ul>
<% else %>
  <h2>There are no Trains</h2>
<% end %>
